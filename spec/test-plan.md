# 测试计划

## 单元测试

- `fixAuthkey`：未编码/已编码 authkey
- `getQuery`：移除翻页参数
- `mergeList`：ID 命中、窗口匹配、空列表
- `analyze`：五星间隔、星级计数

## 集成测试

- 使用 fixture URL 模拟分页响应，验证抓取+合并结果
- 导入 UIGF 后再导出，校验关键字段闭环
- 多账号并存切换验证

## 回归测试断言

- 每池条数不回退
- 最后一条 ID 稳定
- 导入覆盖前自动生成备份文件
